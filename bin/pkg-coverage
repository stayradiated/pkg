#!/usr/bin/env bash

NMDIR="$(cd "$(dirname "$(realpath "${BASH_SOURCE[0]}" )" )" && cd ../../.. && pwd)"

$NMDIR/nyc/bin/nyc.js $NMDIR/@stayradiated/pkg/bin/pkg-test
$NMDIR/nyc/bin/nyc.js report --reporter html
